export default class TournamentSystem{constructor(config={state:null}){this.state=config.state}init(){if(!this.state.get().tournament.ai.length)this.state.patch(s=>s.tournament.ai=Array.from({length:16},(_,i)=>({name:'AI_'+(i+1),pts:Math.floor(Math.random()*1500)})))}update(){}destroy(){}leaderboard(){const s=this.state.get();return [{name:s.profile.username,pts:s.tournament.points},...s.tournament.ai].sort((a,b)=>b.pts-a.pts)}}
