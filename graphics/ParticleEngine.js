export default class ParticleEngine{constructor(config={canvas:null}){this.canvas=config.canvas;this.p=[];this.run=false}init(){if(!this.canvas)return;this.ctx=this.canvas.getContext('2d');for(let i=0;i<90;i++)this.p.push({x:Math.random()*innerWidth,y:Math.random()*innerHeight,v:0.2+Math.random()*0.7});this.run=true;this.loop()}update(){}destroy(){this.run=false}loop(){if(!this.run)return;const c=this.ctx;c.fillStyle='rgba(157,78,221,.25)';for(const p of this.p){p.y+=p.v;if(p.y>innerHeight)p.y=0;c.fillRect(p.x,p.y,2,2)}requestAnimationFrame(()=>this.loop())}}
