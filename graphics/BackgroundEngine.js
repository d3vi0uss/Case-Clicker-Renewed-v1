export default class BackgroundEngine{constructor(config={canvas:null,settings:null}){this.canvas=config.canvas;this.settings=config.settings;this.run=false}init(){if(!this.canvas)return;this.ctx=this.canvas.getContext('2d');this.resize();addEventListener('resize',()=>this.resize());this.run=true;this.loop()}update(){}destroy(){this.run=false}resize(){this.canvas.width=innerWidth;this.canvas.height=innerHeight}loop(){if(!this.run)return;const c=this.ctx,w=this.canvas.width,h=this.canvas.height,t=Date.now()/1000;const g=c.createLinearGradient(0,0,w,h);g.addColorStop(0,'#0c0c12');g.addColorStop(.5,'#120f23');g.addColorStop(1,'#0b0b15');c.fillStyle=g;c.fillRect(0,0,w,h);c.strokeStyle='rgba(122,92,255,.08)';for(let x=0;x<w;x+=45){c.beginPath();c.moveTo(x+Math.sin(t+x*.01)*3,0);c.lineTo(x,h);c.stroke()}requestAnimationFrame(()=>this.loop())}}
